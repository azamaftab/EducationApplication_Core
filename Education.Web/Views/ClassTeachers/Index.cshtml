@model IEnumerable<Education.Domain.ClassTeacher>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Teachers.TeacherName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Class.ClassName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Teachers.TeacherName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Class.ClassName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ClassTeacherID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ClassTeacherID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ClassTeacherID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
